{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core Tauri APIs plus the native dialog picker (`dialog:default`, path strings only). The frontend talks to the engine exclusively through the desktop_* commands; it never spawns processes or touches the filesystem directly.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
